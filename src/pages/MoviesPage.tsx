import {MoviesList, Pagination} from "../components";

const MoviesPage = () => {
    return (
        <div>
            <Pagination/>
            <MoviesList/>
        </div>
    );
};

export {MoviesPage};